//Arrays in javascripts are used for storing multiple list , datas,items etc
//Arrays can be declared in these following ways

var arr = []; //This is an empty array declaration and 
var arr1 = [9,1,2,3] //This is an array containing 3 elements 1 2 2
var arr2 = new Array(5); // Array object is created here
// We can give different types of values in a js array
arr2=["string",'c',1,2.5];
console.log("hello world");
console.log("arr = "+arr);
console.log("arr1 = "+arr1)
console.log("arr2 = "+arr2)
 
//Some usefull methods 
var len =arr1.length; //Gives the length of the array

console.log("length of the arr1 ="+len);
